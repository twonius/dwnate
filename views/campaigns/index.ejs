<% include ../partials/header %>
<!-- Hero -->
<section class="hero">
    <div class="container text-center">
        <div class="col-md-12">
            <p class="tagline">
                <form action="/campaigns" method="GET" id="campaign-search">
                    <div class="col-auto">
                        <label class="sr-only" for="search">Search</label>
                        <div class="input-group col-md-8 col-sm-10 offset-sm-1 offset-md-2 col-xs-12">
                            <input id="search-bar" name="search" type="text" class="form-control" placeholder="Search Representatives" style="padding: 18px; font-weight: bold;">
                        </div>
                    </div>
                </form>
            </p>
        </div>
    </div>
</section>
<!-- /Hero -->
<section class="team" id="team">
    <div class="container">
        <div class="row" id="campaign-grid">
            <% Campaigns.forEach(function(campaign){ %>
            <div class="col-sm-3 col-xs-6">
                <div class="card card-block">
                  <a href="/campaigns/<%= campaign._id %>">
                    <div class="col-sm-4">
                    <img alt="" class="team-img" src="<%= campaign.image %>">
                  </div>
                  <div class="col-sm-8">
                        <div class="card-title-wrap">
                            <span class="card-title"><%= campaign.name %></span>
                        </div>
                        <div class="team-over">
                            <h4 class="hidden-md-down">
                                <a href="/campaigns/<%= campaign._id %>" class="btn btn-primary"> more info </a>
                            </h4>
                        </div>
                      </div>
                    </a>
                </div>
            </div>
            <% }); %>
        </div>
    </div>
</section>
<% include ../partials/footer %>
