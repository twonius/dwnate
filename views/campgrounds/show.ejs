<% include ../partials/header %>

<section>
  <div class="container">
        <h1><%=campground.name%></h1>
        <p><%= campground.description %></p>
  </div>

</section>

<section>
  <div class="col-lg-6">
    <div class="thumbnail">
      <img class="img-responsive" src="<%= campground.image %>">
    </div>
  </div>
  <div class="col-sm-4">
    <div class="row">
        <h3 class="pull-left">Totals</h3>
    </div>
    <br/>
     <!-- <h4>Support</h4>

    <div class="row">
        <h4 class="pull-left">$<%= campground.general %> General Election</h4>
        <div class="text-right">
          <a class="btn btn-success" href="/campgrounds/<%= campground._id %>/comments/new">General</a>
        </div>
      </div>
      <div class="row">
        <h4 class="pull-left">$<%= campground.primary %> Party Primary</h4>
        <div class="text-right">
          <a class="btn btn-success" href="/campgrounds/<%= campground._id %>/comments/new">Primary</a>
        </div>
      </div>
      <br/> -->

    <!-- <h4>DWNATE</h4> -->
    <!-- <div class="row">
        <h4 class="pull-left">-$<%= campground.general %> General Election</h4>
        <div class="text-right">
          <a class="btn btn-danger" href="/campgrounds/<%= campground._id %>/comments/new">General</a>
        </div>
      </div> -->
      <div class="row">
        <h4 class="pull-left">-$<%= campground.primary %> Party Primary</h4>
        <div class="text-right">
          <a class="btn btn-danger" href="/campgrounds/<%= campground._id %>/comments/new">DWNATE</a>
        </div>
      </div>

      <div class="row">
        <h3 class="pull-left">Info</h3>
      </div>
      <div class="row">
        <h5>State: <%= campground.state %> </h5>
        <h5>Party: <%= campground.party %> </h5>
        <h5>Chamber: <%= campground.chamber %> </h5>
      </div>
  </div>




</section>
  </div calss="container">
  <div clas="row">

</div>
    <h4>Trending Tags</h4>
    <div class="well">
      <hr>
      <% campground.comments.forEach(function(comment){ %>
      <div class="row">
        <div class="col-md-12">
          <strong><%= comment.author.username %></strong>
          <span class="pull-right"><%= moment(comment.createdAt).fromNow() %></span>
          <div>
           <%= comment.text %> : -$<%= comment.amount %>

          <hr>
        </div>
      </div>
    </div>
    <% }) %>
  </div>



<script>
  function initMap() {
    var lat = <%= campground.lat %>;
    var lng = <%= campground.lng %>;
    var center = {lat: lat, lng: lng };
    var map = new google.maps.Map(document.getElementById('map'), {
        zoom: 8,
        center: center,
        scrollwheel: false
    });
    var contentString = `
      <strong><%= campground.name %><br />
      <%= campground.location %></strong>
      <p><%= campground.description %></p>
    `
    var infowindow = new google.maps.InfoWindow({
      content: contentString
    });
    var marker = new google.maps.Marker({
        position: center,
        map: map
    });
    marker.addListener('click', function() {
      infowindow.open(map, marker);
    });
  }
</script>
<script async defer src="https://maps.googleapis.com/maps/api/js?key=AIzaSyBkG51lwJtDvYFKTBdRZlhzYg1D4QFsXPU&callback=initMap"></script>
<% include ../partials/footer %>
